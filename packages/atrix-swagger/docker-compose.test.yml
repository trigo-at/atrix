version: '2'
services:
  atrix-swagger:
    build:
      context: .
      args:
        - NPM_TOKEN=${NPM_TOKEN}
    image: trigo/atrix-swagger:latest
    command: 'yarn run test'
    environment:
      - MONGO_SRV=mongodb://mongo
      - NODE_ENV=test
      - NPM_TOKEN
    networks:
      - atrix-swagger_test
    depends_on:
      - mongo

  mongo:
    image: mongo
    networks:
      - atrix-swagger_test

networks:
  atrix-swagger_test:
    driver: bridge
